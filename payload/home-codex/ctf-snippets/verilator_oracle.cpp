#include <cstdint>
#include <cstdio>
#include <vector>
// Copy to work/ and adapt include/module names generated by Verilator.
// #include "Vchip.h"
// static void tick(Vchip* top) { top->CLK = 0; top->eval(); top->CLK = 1; top->eval(); }
int main() {
    // TODO: instantiate Vchip, drive reset/input pins, capture output strobes.
    std::puts("adapt verilator_oracle.cpp to generated top module");
    return 0;
}
